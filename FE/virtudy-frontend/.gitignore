# Logs (로그 파일들)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies (라이브러리 설치 폴더 - 절대 올리면 안 됨!)
node_modules
dist
dist-ssr
*.local

# Editor directories and files (IDE 설정 파일)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Vite specific (Vite 빌드 관련)
.env.local
.env.*.local
.env.*

# TypeScript
*.tsbuildinfo

# Test Coverage (테스트 결과물)
coverage/

# Temporary files (임시 파일)
.tmp/
.temp/

# Vitest
.vitest/

# MediaPipe & AI Models (로컬 테스트용 무거운 모델 파일 제외)
# (public에 필요한 모델은 올려야 하지만, 테스트용 원본은 제외)
/public/mock-data/
/public/models/temp/
venv/
