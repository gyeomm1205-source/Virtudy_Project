server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Vue Router (history mode) 새로고침 404 방지
    location / {
        try_files $uri $uri/ /index.html;
    }
}
